
# 🚀 OmniHR - Multi-Client Time & Attendance System

This project is configured to run entirely in the cloud using **GitHub Codespaces**. No local installation is required.

## 🌟 How to Run it Now (Zero Setup)

1.  **Upload** all these files to your GitHub repository.
2.  Click the green **"<> Code"** button on your GitHub page.
3.  Switch to the **"Codespaces"** tab.
4.  Click **"Create codespace on main"**.
5.  Wait about 2 minutes. A browser-based VS Code will open.
6.  Once the terminal at the bottom stops moving, type:
    ```bash
    npm run dev
    ```
7.  A popup will appear saying **"A service is running on port 3000"**. Click **Open in Browser**.

---

## 🔑 Demo Credentials
- **Admin**: `admin@tech.com` / `any password`
- **HR**: `hr@tech.com` / `any password`
- **Employee**: `emp@tech.com` / `any password`

## 🛠️ Tech Features
- **Cloud Database**: Pre-configured PostgreSQL.
- **AI Engine**: Ready for Gemini API (Add your key to `.env`).
- **Mobile First**: Best viewed in responsive mode.
